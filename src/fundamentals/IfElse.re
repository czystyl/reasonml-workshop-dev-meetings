let isActive = true;

if (isActive) {
  let who = "Luke";

  who ++ " is online";
} else {
  let _search = "";

  "The searched user is offline";
};

let result =
  if (isActive) {
    let who = "Luke";

    who ++ " is online";
  } else {
    let _search = "";

    "The searched user is offline";
  };

let short = isActive ? "Online" : "Offline";